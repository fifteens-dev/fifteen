# Spotify/Apple Music 連携機能 使い方ガイド

## はじめに

このアプリでは、SpotifyまたはApple Musicと連携することで、より高度な音楽機能を利用できます。

## 連携方法

### 1. 設定画面にアクセス

1. アプリを起動
2. 画面下部のタブから **プロフィール** を選択
3. 右上の **⚙️ 設定アイコン** をタップ

### 2. 音楽サービス連携画面を開く

設定画面で以下を選択：
- **設定** セクション
- **連携サービス** (音楽アイコン付き)

### 3. 連携したいサービスを選択

**Spotify** または **Apple Music** の **「連携」** ボタンをタップ

#### Spotifyの場合：
1. Spotifyのログイン画面が開きます
2. Spotifyアカウントでログイン
3. アプリへのアクセス許可を承認
4. 自動的にアプリに戻り、「Spotifyに連携しました」と表示されます

#### Apple Musicの場合：
- Developer Tokenが設定されている場合、自動的に連携されます
- 設定されていない場合は、管理者に連絡してください

### 4. 連携完了

連携が完了すると、ボタンが **「連携中」** に変わり、緑色で表示されます。

## 連携後の機能

### おすすめタブの改善

**投稿 → 楽曲選択画面 → おすすめタブ**

連携前：
- 検索クエリベースの楽曲表示
- 歌詞一致による関係ない楽曲が混入する可能性

連携後：
- **Spotify Recommendations API** による高精度なおすすめ
- ジャンルベースの純粋な推薦アルゴリズム
- 歌詞一致の問題が解消

### キュレーションプレイリスト

連携後、Spotifyがキュレーションしたプレイリストにアクセス可能になります（将来の実装）。

### Apple Musicの場合

- 日本のトップチャートへのアクセス
- Apple Musicのキュレーションプレイリスト
- 高品質なプレビュー音源

## 連携の切り替え

### 別のサービスに切り替える

1. 連携サービス画面で、別のサービスの **「連携」** をタップ
2. 確認ダイアログが表示されます：
   > 「Spotifyとの接続を解除してApple Musicと接続しますか？」
3. **「接続」** をタップ
4. 自動的に切り替わります

### 連携を解除する

1. 連携サービス画面で、現在連携中のサービスの **「連携中」** ボタンをタップ
2. 確認ダイアログが表示されます：
   > 「Spotifyとの接続を解除」
   > 「本当に接続を解除しますか？」
3. **「解除」** をタップ
4. 連携が解除され、基本機能（検索ベース）に戻ります

## トラブルシューティング

### Spotifyログインが失敗する

**症状**: ログイン画面が開かない、または認証が完了しない

**対処法**:
1. Spotifyアプリがインストールされているか確認
2. ネットワーク接続を確認
3. アプリを再起動
4. それでも解決しない場合は、アプリを削除して再インストール

### 「連携に失敗しました」と表示される

**原因**:
- ネットワークエラー
- Spotify側のサーバーエラー
- 認証トークンの期限切れ

**対処法**:
1. ネットワーク接続を確認
2. 時間をおいて再度試す
3. アプリを再起動

### 連携後もおすすめが表示されない

**原因**:
- Recommendations APIが404エラーを返している
- 自動的に検索APIにフォールバックしています

**対処法**:
- これは正常な動作です
- 検索ベースでも高品質な楽曲が取得されます
- 将来のアップデートで改善される予定です

### Apple Musicが使えない

**原因**:
- Developer Tokenが設定されていない
- Developer Tokenの有効期限切れ

**対処法**:
- アプリ管理者に連絡してください
- Developer Tokenの設定には Apple Developer Program（有料）が必要です

## 技術的な詳細

### 認証フロー

#### Spotify (OAuth 2.0 Authorization Code Flow)
1. ユーザーがログインをタップ
2. ブラウザでSpotifyの認証ページを開く
3. ユーザーがアプリを承認
4. カスタムURLスキーム（`fifteenapp://callback`）でアプリに戻る
5. 認証コードを取得
6. アクセストークンとリフレッシュトークンを取得
7. トークンをセキュアストレージに保存

#### Apple Music (Developer Token)
1. アプリ起動時に`.env`からDeveloper Tokenを読み込み
2. APIリクエストにBearerトークンとして追加
3. カタログAPIを呼び出し

### データの保存

- **アクセストークン**: `flutter_secure_storage`で暗号化して保存
- **選択されたサービス**: `shared_preferences`に保存
- **リフレッシュトークン**: `flutter_secure_storage`で暗号化して保存

### API使用の優先順位

1. **認証済みユーザー**: 選択したサービスのOAuth認証API
2. **未認証ユーザー**: Spotify Client Credentials Flow (基本検索)
3. **フォールバック**: 検索クエリベース

この仕組みにより、連携していないユーザーでも基本機能を使用できます。

## よくある質問

### Q: 連携は必須ですか？
A: いいえ、連携なしでも基本的な音楽検索機能は使用できます。

### Q: 両方のサービスに同時に連携できますか？
A: いいえ、一度に1つのサービスのみ連携できます。

### Q: 連携を解除するとデータは削除されますか？
A: はい、アクセストークンとリフレッシュトークンは削除されます。投稿データには影響しません。

### Q: Spotifyの無料プランでも使えますか？
A: はい、Spotify無料プランでも連携可能です。

### Q: Apple Music Developer Tokenとは？
A: Apple Music APIにアクセスするためのJWTトークンです。アプリ管理者が設定する必要があります。

### Q: トークンの有効期限は？
A:
- **Spotifyアクセストークン**: 1時間（自動更新）
- **Spotifyリフレッシュトークン**: 無期限（ユーザーが解除するまで）
- **Apple Music Developer Token**: 最大6ヶ月（管理者が更新）

## サポート

問題が解決しない場合は、以下の情報を添えてお問い合わせください：
- エラーメッセージのスクリーンショット
- 使用している端末（iPhone/Android）
- OSバージョン
- 実行した操作の詳細
