/// Campus Vibe UI の Figma 準拠レイアウト定数
class CampusVibeConstants {
  CampusVibeConstants._();

  /// カード高さ（px）
  static const double cardHeight = 101;

  /// カード左内側パディング
  static const double leftPad = 11;

  /// 「今週末を見る」ボタンの幅
  static const double buttonWidth = 216;

  /// ボタン右端〜画像左端のギャップ
  static const double imageGap = 13;

  /// 画像の左端 X 座標（leftPad + buttonWidth + imageGap）
  static const double imageLeft = leftPad + buttonWidth + imageGap;

  /// 画像左縁のグラデーションフェード幅
  static const double imageFadeWidth = 60;

  /// カード角丸半径
  static const double cardRadius = 19;

  /// 「見るボタン」高さ
  static const double buttonHeight = 22;

  /// 「見るボタン」角丸半径
  static const double buttonRadius = 27;
}
